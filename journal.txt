
=== 2026-04-18 Session 2 ===

## Completed this session

### ETA-9141 PWD Extractor (pdfplumber, no API key)
- Rewrote extract_pwd.py to use pdfplumber, detects old (2021 E/F) vs new (2025+ F/G) form layout
- _CHECK must be regular string not r-string so unicode escapes resolve: "[\u2718\u2717\u2612\u2611\u2714\u2713x]"
- Travel: check checkmark BEFORE "Yes" AND confirm "No" is not also checked
- Degree: word-boundary anchored to prevent "GED" matching inside "High school/GED"
- 18/18 assertions pass both form versions
- FastAPI endpoint: POST /api/extract-pwd (multipart, requires python-multipart)

### BALCA Inline Citation Hyperlinks — BUG FIXED
- useState/useEffect for citeMap was after early returns → violated Rules of Hooks → blank page
- Fixed: moved hook declarations above if(loading)/if(!data) guards in DecisionDetail

### Precedent Decisions (new table: precedent_decisions)
- Columns: volume, citation, party_name, year, body, decision_type, pm_number, adopted_num,
           pdf_url, pdf_path, full_text, search_vector
- 468 EOIR/I&N Dec. precedents ingested (vols 9-29); 284 with text (vols 12-29 complete)
- 15 USCIS-adopted decisions from /Resources/AAO Adopted/ ingested
- Total: 483 rows; decision_type = 'precedent' or 'adopted'
- Scripts: scrape_precedents.py, ingest_supplemental_precedents.py, ingest_adopted.py

### AAO Inline Citation Hyperlinks
- GET /api/precedents/map → 588-key map {"25 I&N Dec. 369": {id, citation, party_name, type}, ...}
  includes "l&N" OCR variant keys and "Adopted Decision YYYY-NN" keys
- GET /api/precedents/{id} → detail; GET /api/precedents/{id}/pdf → serve PDF
- AAODecisionDetail: splits on \d+ [Il]&N Dec\. \d+|Adopted Decision \d{4}-\d+
  Linked: blue underlined clickable; unlinked: dimmed blue
- PrecedentDetail modal component: amber=adopted, blue=precedent, shows full text + PDF link
- precedentMap fetched once on AAO view mount

## Still pending
- Vol 9-11 PDFs blocked by DOJ rate limiting; download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), cross-corpus search, statutes module

=== 2026-04-19 Session 3 ===

## Completed this session

### Cross-corpus Search All (new feature — was "coming soon")
- GET /api/search-all?q=... — UNION ALL across decisions, aao_decisions, regulations_docs, policy_docs
- Results ranked by ts_rank DESC, annotated with corpus field (balca|aao|regulation|policy)
- Count query sums across all four tables
- SearchAllView component: centered landing → collapsed UX, results grouped by corpus with color-coded headers
- Clicking a result routes to the correct existing detail view via openFromSearchAll()
- "Search All" nav tab added (rose accent #fb7185), landing card activated (available: true)
- openFromSearchAll() helper in root App passes query through for BALCA/AAO highlight

### PrecedentDetail modal — BUG FIXED
- precId state existed in AAOSearchView but modal was never rendered → blank clicks
- Fixed: added fixed-position right-side drawer overlay immediately after splitView panel
- Dismissible by clicking the backdrop (outside the drawer)
- Precedent citations in AAO opinion text now correctly open the PrecedentDetail panel

## Still pending
- Vol 9-11 PDFs: blocked by DOJ rate limiting; download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module

=== 2026-04-19 Session 4 ===

## Completed this session

### Read Later feature
- DB migration: ALTER TABLE project_cases ADD COLUMN section TEXT DEFAULT 'saved',
  saved_from_case_number TEXT, saved_from_source TEXT
- API: POST /api/projects/{id}/read-later, GET /api/projects/{id}/read-later,
  DELETE /api/projects/{id}/read-later/{pc_id}
- ReadLaterPopup component: hover over any linked case number in an opinion text
  (BALCA inline citations + AAO precedent citations) → 350ms delay popup appears
  showing project list; click to toggle save/unsave into that project's Read Later
- Popup shows "while reading {fromCaseNumber}" context line before the project list
- ProjectDetail: new "Read Later" tab between Cases and Notes
  - Each card shows corpus badge (BALCA/AAO), case number, employer, date, outcome
  - "saved while reading {case}" provenance line with correct accent color
  - Click card to open the decision; ✕ to remove from Read Later
- Popup uses 200ms hide delay so user can mouse from the linked text into the popup
  without it closing

## Still pending
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module

=== 2026-04-19 Session 5 ===

## Completed this session

### Citation Graph (new feature)
- GET /api/search/citation-graph?q=...&limit=40
  - Step 1: top N matching BALCA decisions (primary nodes, ranked by ts_rank)
  - Step 2: citation edges between primaries via citations table
  - Step 3: secondary nodes — cases cited by primaries but not in search results
  - Secondaries filtered to cited_by_count >= 2 (or 1 if fewer than 3 qualify)
  - Returns {nodes, edges, primary_count, secondary_count, query}
- CitationGraphView component:
  - D3 v7 force simulation loaded from CDN (window.d3)
  - Directed arrows on edges (SVG marker)
  - Node size: primary scaled by ts_rank, secondary by cited_by_count
  - Node color by outcome (Affirmed=green, Reversed=red, Remanded=yellow, Dismissed=gray)
  - Primary nodes: full opacity + white stroke; secondary: dimmed fill
  - Drag-to-reposition nodes, scroll-to-zoom, pan
  - Hover tooltip (bottom-left): case number, employer, date, outcome, tier badge
  - Click → selected node panel (top-right): full details + "Open decision →" button
  - Opening a decision navigates to BALCA view with that decision pre-selected
- "Graph" nav tab (green accent), landing card added

## Still pending
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module

=== 2026-04-19 Session 6 ===

## Completed this session

### In-document search (Find in document)
- useDocSearch() hook: manages term, active term, match index, match count, ref array
  for each match span, scroll-to-active-match on index change
- InDocSearch component: collapses to "Find" button; expands to input + ↑↓ nav +
  match counter (N/total, red when 0) + × close; keyboard: Enter=next,
  Shift+Enter=prev, Escape=close; accent color matches each corpus
- HighlightedText component: splits text on doc search term, renders dimmed marks
  for all matches + solid amber + outline for the active match; useEffect inside
  fixed to avoid conditional hook violation
- Wired into DecisionDetail (amber), RegulationDetail (agency color),
  PolicyDetail (source color)
- DecisionDetail: in-doc search takes priority over citation links / query
  highlights when active; normal rich render resumes on close
- Find button appears in top-right of each document header alongside PDF button

=== 2026-04-19 Session 7 ===

## Completed this session

### Search operators (websearch_to_tsquery)
- Replaced plainto_tsquery with websearch_to_tsquery across all 30 occurrences
  in api.py — covers BALCA search, AAO search, regulations search, policy search,
  cross-corpus search-all, and citation graph
- Supported operators now:
    "exact phrase"       → phrase search
    -excludeterm         → exclude a term
    termA OR termB       → either term
    "phrase" -exclude    → combinations
- Placeholder text updated in all 4 search inputs to hint at operator syntax

### Composite ranking (citation authority + text relevance)
- BALCA search ORDER BY upgraded to:
    ts_rank * 0.7 + log(1 + citation_count) * 0.3
  where citation_count = number of times that decision is cited by other decisions
- AAO search ORDER BY upgraded with the same formula
- Citation graph seed query upgraded with the same formula
- Regulations + Policy keep pure ts_rank (no citations table to join)
- log() compression prevents highly-cited cases from dominating;
  70/30 weight keeps text relevance primary

## Still pending
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module

=== 2026-04-19 Session 8 ===

## Completed this session

### Experience Verification Letter modal (PERM Comparer)

New "Verify Experience" button added to the PERM Comparer toolbar. Opens a full-screen
side-by-side modal with:

Left panel — PWD Minimum Requirements text (from the Reference textarea), with keywords
  auto-highlighted based on skills found in the letter and requirements text.

Right panel — Experience verification letter, with:
  - PDF upload → Anthropic API extracts employer name, job title, dates, duties, skills,
    and full text entirely client-side (no backend changes needed)
  - Metadata bar shows employer, job title, employment period, and computed months/years
  - "Analyze vs PWD" button sends the letter text + requirements to Claude and returns
    two lists: requirements addressed vs. requirements not found, plus a brief summary
  - "Save Time" button marks the letter as saved; a running total of saved months across
    all saved letters is displayed in the header
  - Multiple letters supported via tab strip; letters can be removed individually
  - Closing and reopening the modal preserves all uploaded letters (state lives in
    PermComparer, not the modal)
  - Clear All resets letters along with everything else

## Future work — AUTOMATED EXPERIENCE COMPARISON (TODO)

Implement automated total-time comparison against PWD requirements:
  - Sum saved months across all letters and compare against the years required by the PWD
  - This is non-trivial because PERM language often phrases requirements as "3 years of
    experience in the field" but lists skills (Python, HTML, CSS) that appear to require
    3 years each — when in practice only 3 total years in the field is required and the
    skills just need to appear somewhere in the letters
  - Any automated comparison must account for this language distinction to avoid false
    flags; suggest a two-pass approach: (1) flag if total time is below threshold, (2)
    separately verify each listed skill appears in at least one letter, without asserting
    time-per-skill
  - Consider adding a "time goal" input so users can set the target years manually rather
    than trying to parse it from the PWD text

## Still pending
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module
- Recruitment document comparison (next after experience letters)

=== 2026-04-19 Session 9 ===

## Completed this session

### Drag-and-drop PDF extraction for Job Description boxes

Both Job Description textareas replaced with DropTextarea component:
- Left (Reference): drag and drop a PWD PDF → extracts job duties section only
- Right (Comparison): drag and drop an experience verification letter PDF → extracts
  full body text including duties, responsibilities, skills, and technologies
- Amber border glow + "Drop PDF to extract text" overlay while hovering
- Spinner overlay with "Extracting text…" during API call; textarea disabled while
  extracting so nothing gets clobbered mid-flight
- Label text updated to tell users they can paste OR drag and drop
- Placeholder text explains both options
- Plain paste still works exactly as before

### CORS fix — Anthropic API proxy

All three direct client-side fetch calls to https://api.anthropic.com/v1/messages
were blocked by CORS (intentional on Anthropic's end). Fixed by routing through the
existing /api/claude proxy endpoint in api.py, which adds the API key server-side.

Affected calls:
  1. DropTextarea.extractPdf — PDF drag-drop extraction in Job Description boxes
  2. ExperienceVerificationModal.uploadLetter — experience letter PDF upload
  3. ExperienceVerificationModal.analyzeAgainstPwd — requirements comparison

The /api/claude proxy (already in api.py) reads ANTHROPIC_API_KEY from .env.
Action required: set ANTHROPIC_API_KEY in perm-research/.env to enable AI features.

## Still pending
- Set ANTHROPIC_API_KEY in .env
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module
- Recruitment document comparison (next after experience letters)
- Automated experience time comparison (see Session 8 TODO)

=== 2026-04-19 Session 10 ===

## Completed this session

### Removed all AI/Anthropic API calls from the frontend

Client consent and internal approval would be required before sending any data to
a third-party AI API. All four AI-powered features have been replaced with
server-side pdfplumber + regex processing. No data leaves the local backend.

### New file: extract_experience_letter.py

Rule-based pdfplumber extractor for experience verification letters. Extracts:
  - employerName    — first letterhead line
  - employerTitle   — signer's title from signature block
  - beneficiaryName — name found in employment sentence
  - jobTitle        — parsed from "employed as / position of / served as" triggers
  - startDate / endDate — as written in the letter
  - startParsed / endParsed — normalised to YYYY-MM-DD (ISO)
  - months          — computed duration
  - duties          — body paragraph(s) after duties/responsibilities heading
  - skills          — tech/professional keywords matched against a curated list
  - fullText        — raw extracted text

Handles named months, MM/YYYY, MM/DD/YYYY, YYYY-MM formats.
Recognises "Present" / "current" / "ongoing" as end date.

### New API endpoints in api.py

  POST /api/extract-text
    Generic PDF → plain text via pdfplumber. Used by drag-drop boxes.

  POST /api/extract-experience-letter
    Structured letter parsing via extract_experience_letter.py.
    Used by the Verify Experience modal upload.

### Frontend changes

  DropTextarea (Job Description boxes):
    - Previously called /api/claude (Anthropic) with base64 PDF
    - Now POSTs multipart form to /api/extract-text (pdfplumber, local)

  ExperienceVerificationModal (letter upload):
    - Previously called /api/claude with base64 PDF + AI prompt
    - Now POSTs multipart form to /api/extract-experience-letter (pdfplumber, local)

  "Analyze vs PWD" button removed entirely.
    - This feature requires AI reasoning and cannot be replaced with rule-based logic
    - Noted in Session 8 TODO for future implementation once AI use is approved

  Removed unused client-side helpers: parseFlexDate, dateDiffMonths
    (date math now done in Python backend and returned as ISO strings + integer months)

  /api/claude proxy endpoint retained in api.py but no longer called from the frontend.

## Still pending
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module
- Recruitment document comparison
- Automated experience time comparison (see Session 8 TODO)
- "Analyze vs PWD" (requires AI approval — see Session 8 TODO)

=== 2026-04-19 Session 11 ===

## Completed this session

### Restored smart drag-and-drop behavior

**Left box (Job Description Reference) — drag a PWD PDF:**
  Calls /api/extract-pwd (pdfplumber, same as Load PWD button).
  Populates all fields: job title, city, state, telecommute, job description,
  primary degree, minimum requirements, and PWD wage.
  Identical result to using the Load PWD button.

**Right box (Job Description Comparison) — drag an experience letter PDF:**
  Calls /api/extract-experience-letter (pdfplumber + regex).
  Puts duties/full text into the comparison textarea.
  Stores the full parsed letter object (employer, dates, months, skills, fullText)
  in droppedLetter state so the modal can use it immediately.

**Verify Experience button:**
  If a letter was dropped into the comparison box and not yet in the letters list,
  it is automatically added when the modal opens — no separate upload step needed.
  Button turns amber when a dropped letter is waiting.

### Keyword matching panel in the modal (no AI)

The modal now runs a pure client-side keyword check:
  - Extracts all meaningful words (3+ chars, non-stop-words) from the Minimum
    Requirements reference text (mrRef)
  - For each keyword, checks case-insensitively whether it appears anywhere in
    the letter's fullText
  - Displays each keyword as a green ✓ pill (found) or red ✗ pill (missing)
  - Shows a summary count: "X of Y requirement keywords found in this letter"

Both the PWD panel and the letter text panel highlight those same keywords in amber
so the reviewer can see exactly where each term appears in context.

Success criterion: each requirement word from the PWD appears somewhere in the letter.
This matches the stated requirement — not trying to verify years-per-skill, just
confirming the terms are present.

## Still pending
- Vol 9-11 PDFs: download manually then run extract/ingest loop
- Letter assistant (NIW/EB-1A RAG), statutes module
- Recruitment document comparison
- Automated experience time comparison (see Session 8 TODO)
- "Analyze vs PWD" AI feature (requires approval — see Session 8 TODO)

=== 2026-04-19 Session 12 ===

## Notes for tomorrow

### Wage Transparency Law Checker (TODO)
Add a wage transparency law checker to the PERM Comparer based on the job location
(city/state) extracted from the PWD. Many states and cities have wage range disclosure
laws that require job postings to include a salary range. The checker should:
  - Use the city/state already populated by the PWD extractor
  - Flag whether that jurisdiction has a wage transparency law in effect
  - Indicate what the law requires (e.g. range in posting, on request, etc.)
  - Ideally note the effective date and any relevant thresholds (employer size, etc.)

### Experience Verification Letter comparison (longer project)
The current keyword-match approach in the Verify Experience modal is a starting point
but needs a more thoughtful redesign. Defer to a future session.

=== 2026-04-19 Session 13 ===

## Completed this session

### Equal Pay Transparency (EPT) card added to PERM Comparer

New card appears between the PWD Wage Check and the Summary, auto-populating
from the state already extracted from the PWD.

Source data: AILA Practice Pointer — Guide to Equal Pay Transparency Laws
(Updated October 2025), AILA Doc. No. 25110603.

Covers 12 jurisdictions: CA, CO, HI, IL, MD, MA, MN, NJ, NY, VT, DC, WA.

Card shows:
  - Effective date and covered employer threshold for the jurisdiction
  - Posting requirement (what must be in the ad)
  - Wage range requirement, highlighted green if a range is already entered
    in the wage fields, amber if no range has been entered yet
  - Benefits description required flag (amber warning) for states that require
    more than just a salary range: CO, IL, MD, MN, NJ, WA
  - Long-arm reach warning (red) for CA and NJ
  - Remote/WFH coverage note when telecommute is set to "yes"
  - Source citation and reminder to verify with local employment counsel
  - "No EPT law on record" message for states not in the guide
  - Prompt to enter state if state field is empty

States not covered by the guide: all other states — card shows no-law message.
Note: laws change frequently; card includes explicit disclaimer.

=== 2026-04-19 Session 14 ===

## Completed this session

### Fixed PWD extraction for new-layout ETA-9141 (P-100 series)

Root cause: pdfplumber outputs form table cells differently depending on the PDF
renderer. Three layouts were observed and are now handled:

  Layout A (most common in new form): labels row + values row
    "3. City *    4. State *    5. County *\nRichardson    TX    Dallas County"
    Fixed by matching the label row pattern then splitting the value row on 2+ spaces.

  Layout B: each label on its own line, value on next line
    "3. City *\nRichardson\n4. State *\nTX"
    Already worked; kept as fallback.

  Layout C: label and value on same line (table cell)
    "3. City *    Richardson"
    Added as second fallback.

Additional fixes in _extract_new:
  - Job title: now handles same-line placement ("1. Job title *   Data Scientist II")
    in addition to next-line placement
  - All searches now use body = pages[1:7] instead of hard-coded page indices,
    making extraction robust to cover pages that shift the page offset
  - Wage search extended to pages[4:7] range instead of a single page index
  - Duplicate _parse_unit block removed from _extract_new

Same three-layout fix applied to _extract_old city/state extraction.
Old form job title also updated to handle same-line placement.
Old form education and special requirements now search body instead of p3.

Verified with real PWD (P-100-25202-188629, Chewy Inc., Data Scientist II):
  jobTitle: "Data Scientist II" ✓
  city: "Richardson" ✓
  stateVal: "TX" ✓
  travel: "yes" (telecommuting) ✓
  primDeg: "Master's" ✓
  mrRef: full addendum text ✓
  pwdWage: "$118,893 / Year" ✓


## $(date '+%Y-%m-%d') — Scheduled scraping + search fixes

### BALCA date-search bug fixed
- asyncpg requires `datetime.date` objects for DATE columns, not raw strings.
  Passing `"2026-01-01"` caused a 500 crash whenever date-only searches were used.
  Fixed by adding `from datetime import date as _date` and calling `_date.fromisoformat()`
  on `date_from` / `date_to` in both `/search` and `/aao/search` endpoints.
- Added `try/catch/finally` to both frontend search functions so the spinner
  always clears and errors surface in the UI instead of hanging forever.

### Docketing notices filtered from search
- 672 procedural "Notice of Docketing, Briefing Schedule, and Order Requiring
  Electronic Filing" documents identified and tagged `doc_type = 'docketing_notice'`
  in the decisions table.
- API now excludes them by default; advanced filters panel has a checkbox to
  include them when needed (e.g. to check when a recent case was filed).
- 62 cases that contained the docketing header but also had a real outcome were
  left as `doc_type = 'decision'` — these are consolidated PDFs with both.

### Scheduled sync set up
Two files created:
- `sync_casebase.sh` (repo root) — the sync script. Runs in order:
    1. BALCA scraper CLI (venv/bin/python) — fetches latest from DOL search
    2. ingest_rag.py --corpus balca — pushes to Postgres
    3. Inline Python — tags any newly ingested docketing notices
    4. ingest_aao.py — re-indexes recent AAO decisions
  Includes a lock file to prevent overlapping runs.
  Logs to ~/Library/Logs/casebase_sync.log.

- `~/Library/LaunchAgents/com.casebase.sync.plist` — launchd job.
  Schedule: every Sunday at 2:00 AM.
  Loaded and verified with `launchctl list | grep casebase`.

To trigger a manual sync:   bash ~/Documents/GitHub/balca-perm-scraper/sync_casebase.sh
To check the log:           tail -50 ~/Library/Logs/casebase_sync.log
To unload the schedule:     launchctl unload ~/Library/LaunchAgents/com.casebase.sync.plist
To reload after editing:    launchctl unload ... && launchctl load ...

=== 2026-04-27 Session ===

## Completed this session

### USCIS Evidence Checklist Scraper + Ingest Pipeline (new corpus: uscis_checklists)

**Scraper** — `perm-research/scrape_uscis_checklists.py`
- Targets 10 USCIS checklist pages: I-129, I-765, N-400, I-140, I-485, I-539, I-130, I-131, I-290B, N-600
- Cascade fetch: httpx (3 user-agents) → wget subprocess → Playwright fallback
- Two page-format parsers:
  - Accordion (USWDS usa-accordion__button, details/summary, heading+sibling) — for I-129, I-765, etc.
  - Table format — for N-400, N-600-style static pages
  - Plain-text fallback on main content area
- JSON output: form, classification, requirement_type="initial_evidence", content, source_url, scraped_date
- Saves combined `data/uscis_checklists.json` + per-form files for inspection
- Usage: `python3 perm-research/scrape_uscis_checklists.py [--form I-129] [--dry-run]`

**Ingest script** — `perm-research/ingest_uscis_checklists.py`
- Follows same pattern as ingest_uscis.py (psycopg2, dotenv, --ingest/--embed/--status/--reset)
- `ensure_corpus_allowed()` patches rag_chunks_corpus_check to add 'uscis_checklists'
- Each form+classification = one source document; chunked at 800 tokens / 80-token overlap
- source_id: slug of form__classification; form_type: form number; cfr_citation: source_url
- Header prepended to first chunk for standalone retrieval context
- Usage: `python3 perm-research/ingest_uscis_checklists.py --ingest --embed`

**API endpoints** — `app/api.py`
- GET /api/checklists               — list all form/classification pairs
- GET /api/checklists/search?q=...  — full-text search (optional &form= filter)
- GET /api/checklists/{form}        — all classifications for a form, reassembled text
  Note: /search defined before /{form} to avoid FastAPI route shadowing
- /api/search-all UNION extended: uscis_checklists corpus now included in global search
  (both the ranked results UNION and the count subquery)

**DB change required before first ingest:**
  Run `python3 ingest_uscis_checklists.py --ingest` — the script auto-patches the
  rag_chunks_corpus_check constraint to add 'uscis_checklists'.

**Run order:**
  1. `python3 perm-research/scrape_uscis_checklists.py`
  2. `python3 perm-research/ingest_uscis_checklists.py --ingest`
  3. `python3 perm-research/ingest_uscis_checklists.py --embed`


---

## Session: 2026-05-04

### Kurzban's Gap Analysis

Systematically read all 13 chapters + appendices of Kurzban's Immigration Law Sourcebook 19th Ed. (2024) from OneDrive to identify publicly available legal sources not yet in Casebase. Full gap analysis saved to `/mnt/user-data/outputs/kurzban_gap_analysis.md` and updated to reflect that FAM, Visa Bulletins, and BLS OEWS are already ingested.

Top remaining gaps (priority order):
1. BIA Practice Manual
2. USCIS Policy Memos/Alerts (standalone PA-/PM- series)
3. Circuit court opinions (CourtListener)
4. EOIR Policy Memos
5. CFR Title 29 (immigration-relevant parts)

---

### CourtListener Pipeline — `scripts/scrape/scrape_courtlistener.py` + `scripts/ingest/ingest_courtlistener.py`

**Exploration:** Wrote and ran `scripts/scrape/explore_courtlistener.py` to characterize the CourtListener API v4. Key findings:
- Auth token works; uses `Token` header
- `/search/?type=o` endpoint is fast (0.6s); `/clusters/` endpoint times out (>60s) — use search
- `plain_text` often empty; `html_with_citations` has full text — strip HTML tags
- Opinions filterable by cluster ID via `/opinions/?cluster=<id>` (fast, avoids extra cluster fetch)
- No explicit rate-limit headers; 429s enforced silently; 502s are transient and retry-safe
- `count` field quirk: returns URL string when `fields=` param is passed — don't use for counts
- ~57,977 total immigration-related opinions across 13 circuits from 2000-present

**Scraper design decisions:**
- Pagination: `/search/` endpoint with cursor (not `/clusters/` — too slow)
- Text fetch: `/opinions/?cluster=<id>` directly (2 API calls per opinion, no extra cluster hop)
- Text field priority: `plain_text` → `html_with_citations` → `xml_harvard` → `html`
- Rate limiting: 0.5s sleep between requests, 65s backoff on 429, retry on 502/timeout
- Storage: one JSON file per cluster at `~/courtlistener_opinions/<court_id>/<cluster_id>.json`
- Idempotent: skips files already on disk

**Ingest design:**
- Corpus: `court_opinions`
- source_id: cluster_id (TEXT)
- source_label: "Case Name (Citation) [COURT]"
- source_date: date_filed
- Standard 800-token / 80-token overlap paragraph chunking
- `ensure_corpus_allowed()` uses `NOT VALID` constraint to avoid scanning existing rows
- `--ingest` / `--embed` / `--status` / `--reset` / `--court` / `--limit` flags

**Status:** Scrape running as of 2026-05-01, currently mid-ca2 after completing scotus (~13,500 opinions). All 13 circuits queued from 2000-present. Ingest + embed deferred until scrape completes. Note: broad court+date filter (not keyword filter) — will capture all published opinions, not just immigration-specific. Consider adding keyword filter at ingest time to reduce non-immigration noise.

**Files:**
- `scripts/scrape/scrape_courtlistener.py`
- `scripts/scrape/explore_courtlistener.py`
- `scripts/ingest/ingest_courtlistener.py`
- Data dir: `~/courtlistener_opinions/<court_id>/`
- Token: `177d15d21ae11462542b0029637d457c9cb229eb` (also in .env as COURTLISTENER_TOKEN)

**Run commands:**
```
venv/bin/python3 scripts/scrape/scrape_courtlistener.py --scrape --since 2000-01-01
venv/bin/python3 scripts/scrape/scrape_courtlistener.py --status
venv/bin/python3 scripts/ingest/ingest_courtlistener.py --ingest
venv/bin/python3 scripts/ingest/ingest_courtlistener.py --embed --batch-size 8
```

---

### CFR Title 29 — `scripts/scrape/download_cfr_title29.py`

Downloaded 6 immigration-relevant Title 29 CFR parts missing from Casebase. Note: eCFR no longer serves PDFs via API — switched to XML versioner endpoint and convert to plain text.

**Parts downloaded** (saved as .txt to OneDrive Regulations folder):
- Part 1   — Davis-Bacon wage predeterminations (53,662 chars)
- Part 18  — OALJ rules of practice (H-1B/H-2 enforcement hearings) (356,379 chars)
- Part 516 — FLSA recordkeeping (53,949 chars)
- Part 541 — White-collar exemptions / specialty occupation context (116,930 chars)
- Part 778 — FLSA overtime compensation (298,846 chars)
- Part 810 — USMCA high-wage labor value content (70,366 chars)

Parts 500–508 (H-2A/H-2B) were already present as PDFs.

**ingest_regulations.py changes:**
- Added `.txt` file support alongside existing `.pdf` handling
- Added all 29 CFR part names to `PART_NAMES` dict
- `AGENCY_MAP[29]` was already present

**Ingest result:** 126 files processed, 0 errors (includes all new Title 29 parts).
The new parts are now in `regulations_docs` but not yet RAG-chunked into `rag_chunks`.
Run the regulations RAG ingest script to chunk + embed when ready.

**Files:**
- `scripts/scrape/download_cfr_title29.py` — download script
- `scripts/ingest/ingest_regulations.py` — updated with txt support + part names

=== 2026-05-08 Session ===

## Completed this session

### INA (pre-PERM) scraper support

`balca_perm_scraper/pipeline.py`:
- Added `INA_FILTER = "case_type eq 'INA'"` alongside existing `PERM_FILTER`
- Added `INA_FISCAL_YEARS` list spanning 1985–2008 (INA cases predate PERM
  transition; also covers 2006–2008 for any stragglers)
- `build_search_body()` now accepts `case_filter` param (defaults to PERM_FILTER)
- `collect_search_results()` now accepts `ina: bool` flag — when True, uses
  INA_FILTER and INA_FISCAL_YEARS; run logged as "INA" in console output

`balca_perm_scraper/cli.py`:
- `search` command gets `--ina` flag: `balca-perm-scraper search --ina --max-pages 50`
  will scrape all INA case_type records from OALJ Azure Search

normalize.py DOCKET_RE already handles INA: `\b(\d{4})-?(PER|INA|TLN)-?(\d{3,6})\b`

Run INA scrape:
  cd /Users/Dad/Documents/GitHub/Casebase
  DOL_AZURE_QUERY_KEY=<key> venv/bin/python3 -m balca_perm_scraper search --ina --max-pages 50

### PDF button fix

Root cause: `PDF_BASE_PATH` env var was not set, so the API used its default
`/Users/Dad/Documents/GitHub/balca-perm-scraper/data/raw/pdfs` — that repo only
has a monitoring folder. Actual PDFs are at
`/Users/Dad/Documents/GitHub/Casebase/data/raw/pdfs`.

Fix: added `PDF_BASE_PATH=/Users/Dad/Documents/GitHub/Casebase/data/raw/pdfs`
to `/Users/Dad/Documents/GitHub/Casebase/.env`.
API server restarted to pick up the change.

### Inline "New project" in SaveToProject dropdown

`SaveToProject` component in `frontend/src/App.jsx`:
- Added `creating`, `newName`, `newColor`, `newNameRef` state/refs
- Replaced "Go to Projects tab to create new" hint with a "+ New project" button
- Clicking it expands an inline form: name input + 8-color swatch + "Create & add"
  / "Cancel" buttons
- `createAndAdd()` POSTs to /api/projects, then immediately POSTs the case to
  the new project's /cases endpoint, and updates local `projects`/`saved` state
  so the new project appears checked immediately without a reload
- Enter key submits the form; Escape cancels it; focus goes to the name input
- Added `PROJECT_COLORS_INLINE` const (same palette as ProjectsView's
  PROJECT_COLORS) to avoid circular reference with that const



════════════════════════════════════════════════════════════════════
2026-06-10 — MODULARIZATION REFACTOR (api.py + App.jsx) — Claude session
════════════════════════════════════════════════════════════════════

PRE-REFACTOR DISCOVERY: dev and deploy repos had silently diverged.
Deploy (casebase-app) had features built directly in it that dev lacked:
kanban system (api/kanban.py + KanbanView.jsx), docx extraction
(_docx_to_text), permCompliance.js recruitment audit, /api/ask tuning.
Dev had newer work deploy lacked (uscis_checklists/form_instructions/
dol_faqs corpora in search-all, SaveToProject inline project creation).
DANGER that was defused: sync-from-dev.sh rsyncs app/→api/ with --delete
and would have DELETED kanban.py and clobbered all deploy-only work on
next run.

RESOLUTION: three-way merge using git history. Found last-sync base
commits (1061bcf for api.py, 6adeae8 for App.jsx), ran git merge-file —
ZERO conflicts both files. Backported kanban.py, KanbanView.jsx,
permCompliance.js into dev. Commit c46f8d9. Dev is now the superset and
single source of truth again. RULE GOING FORWARD: never edit casebase-app
directly; always edit Casebase and sync.

BACKEND SPLIT (commit 3e48600): app/api.py (3,804 lines) →
  - app/core.py (389): env config, database, q(), ensure_operational_schema,
    search logging, auth helper, PDF/GCS helpers, embed_query. Defines
    __all__ so routers do `from core import *`.
  - app/routers/: balca, projects, aao, regulations, policy, search_all,
    extraction, ina, checklists, rag, oflc, visa_bulletin (12 routers,
    93 routes moved).
  - app/api.py (56 lines): assembly only — lifespan, CORS, kanban wiring,
    include_router loop, static serving. sys.path shim makes both
    `uvicorn api:app` (Docker) and `uvicorn app.api:app` (dev) work.
  VERIFIED: route set before/after byte-identical (110 routes).

FRONTEND SPLIT (commit ebec238): frontend/src/App.jsx (4,732 lines) →
  common.jsx (shared: Badge, Spinner, ReadLaterPopup, InDocSearch,
  HighlightedText, SaveToProject, hooks), BalcaViews, ProjectsViews,
  RegulationPolicyViews, PermComparer, AaoViews, SearchAllView,
  CitationGraphs, AskView + 328-line App.jsx shell. All cross-module
  imports auto-generated and verified acyclic (views import only from
  common). Vite build passes. LESSON: lowercase helper functions cluster
  just above the component that uses them — module boundaries must be
  set at the helper block start, not the component start.

VENV NOTE (this machine, MacBook Pro): venv lacked API deps; installed
app/requirements.txt into Casebase/venv so import tests run locally.

NOT YET DONE: sync-from-dev.sh + docker compose up --build -d (deploy);
live uvicorn smoke test against a running Postgres.

=== 2026-06-18 Session ===

## Completed this session

### Visa Bulletin Ingest — June 2026 & July 2026

Added two missing monthly visa bulletins to both the local system DB and the live
Docker Casebase DB.

**Method:** Fetched both bulletin pages directly (travel.state.gov), parsed tables
using the existing scrape_visa_bulletins.py logic, upserted via asyncpg into the
local DB, then transferred to Docker via CSV COPY.

**Results:**
- June 2026: 120 rows (EB + FB × final_action + dates_for_filing × 5 chargeabilities)
- July 2026: 120 rows — same structure

**Notable July 2026 data points (practitioner-relevant):**
- EB-2 India: UNAVAILABLE (pro-rated FY2026 limit reached; DOS notes October advance expected)
- EB-5 Unreserved India: UNAVAILABLE (same reason)
- EB-1 India: 15OCT22 (retrogressed from 15DEC22 in June)
- EB-1 China: 01JUN23 (advanced from 01APR23 in June)
- EB-3 ALL: 01AUG24 (advanced from 01JUN24 in June)

**Commands used:**
```
# Local ingest
venv/bin/python3 /tmp/ingest_june_july_bulletins.py

# Docker transfer
docker cp /tmp/vb_new.csv casebase-db:/tmp/vb_new.csv
docker exec -i casebase-db psql -U perm -d perm_decisions
  → CREATE TEMP TABLE vb_import ...; \COPY ...; INSERT ... ON CONFLICT DO UPDATE

# Verify
docker exec -i casebase-db psql -U perm -d perm_decisions -c
  "SELECT bulletin_date, bulletin_title, COUNT(*) FROM visa_bulletin
   WHERE bulletin_date >= '2026-05-01' GROUP BY 1,2 ORDER BY 1"
```

**No app code changes needed** — the existing `/api/visa-bulletin/latest` endpoint
automatically returns July 2026 as the most recent bulletin since it queries
`MAX(bulletin_date)`.

=== 2026-06-18 Session ===

## OEWS Wage Prediction Model — Data Collection Phase

### Research: OFLC wage calculation methodology
- OFLC does NOT publish OEWS percentile values directly. BLS provides custom tabulations:
  - Level I = mean of lowest-paid 1/3 of workers (~17th pctile)
  - Level IV = mean of highest-paid 2/3 of workers (~67th pctile)
  - Level II = L1 + (1/3)(L4 - L1)
  - Level III = L1 + (2/3)(L4 - L1)
- BLS uses rolling 6-panel MB3 model (3 years of semiannual surveys)
  - May 2025 OEWS uses panels: May25, Nov24, May24, Nov23, May23, Nov22
  - Older panels are aged forward to May reference date using ECI as the aging factor
  - ECI series used: private industry wages & salaries by occupational group
- Published BLS percentile figures (10/25/50/75/90) are NOT identical to OFLC's L1-L4
- May 2025 BLS OEWS release: May 15, 2026 — this IS the source for July 2026 OFLC update
- NPRM (ETA-2026-0001, 91 FR 15454, Mar 27 2026): would raise pctiles to 34/52/70/88
  - Comment period closed May 26, 2026. Not yet finalized. July 1 update uses current method.

### Data pulled to /tmp/oews_model/
- ECI data (via BLS API):
  - /tmp/oews_model/eci/eci_clean.csv (264 rows)
  - Series: private all workers, mgmt/prof, service occupations — quarterly 2018-2026
  - Both annual % change series and index values (Dec 2005=100)
  - Latest: Q1 2026 — priv all = 3.4%, mgmt/prof = 3.4%
- OFLC ALC_Export files (5 years):
  - 2021-22, 2022-23, 2023-24, 2024-25, 2025-26
  - Each ~27-30MB, 450k rows (SOC x MSA x GeoLvl)
  - Hourly wage rates for L1-L4 + Average
- BLS OEWS API (blocked by Akamai for historical years):
  - Only current year (2025) available via API
  - BLS download.bls.gov server blocks automated access
  - Decision: use OFLC files as primary data (they ARE derived from OEWS)

### Next steps
- Build validation model: for each OFLC year-pair, compute YoY OFLC delta
  and compare against ECI growth for corresponding panels
- Test whether ECI-weighted aging model predicts OFLC changes better than naive YoY
- Apply model to current ECI data (Q1 2026 = 3.4% mgmt/prof) to project July 2026 floors
- Build the actual prediction tool/widget

=== 2026-06-18 Session (continued) ===

## OEWS Wage Prediction Model — Validation & Cross-Check

### Cross-validation findings
- BLS national data vs OFLC MSA-mean comparison showed ~17-20% gap (BLS higher)
- Root cause: geographic composition effect, NOT formula error
  - BLS national includes all workers; OFLC covers only MSA-level workers
  - Mean of MSA-level OFLC L1s ≠ national BLS L1 (small metros drag mean down)
- OFLC internal formula confirmed: Avg = (L1 + 2*L4)/3 holds to 0.17% (rounding only)
- Correct validation: OFLC YoY change vs ECI YoY change (apples-to-apples)

### OFLC-to-ECI residuals (mean across 10 SOCs):
- 2021-22→2022-23: -2.0% residual (anomalous — ECI spiked, OFLC lagged)
- 2022-23→2023-24: +0.7% (OFLC > ECI by small margin)
- 2023-24→2024-25: +1.3% (OFLC > ECI)
- 2024-25→2025-26: +0.1% (near-perfect fit)
- 3-year calibration (excl anomalous): +0.7%

### Final July 2026 projection method
- Base: Actual OFLC 2025-26 L1/L2/L3/L4 for each MSA×SOC (from ALC_Export.csv)
- Growth factor: ECI Q2 2025→Q2 2026 (est.) + panel rotation lift + calibration
  - mgmt/professional SOCs: +5.61%
  - all-occupations SOCs: +5.85%
- Prediction uncertainty: ±1.5% → ±$1,500-4,000 on typical L2 floor
- Output saved: /tmp/oews_july2026_final.json (14 SOCs × 20 MSAs)

### BLS OEWS files now available locally
- Kellen downloaded: national_M2021_dl.xlsx, national_M2022_dl.xlsx,
  national_M2024_dl.xlsx, national_M2025_dl.xlsx
- national_M2023_dl.xlsx missing (not uploaded) — 4-year validation still valid
- Files at: /mnt/user-data/uploads/ (Claude container only, not Mac)
- TODO: save these to Casebase repo or a permanent location on Mac

### Key insight for metro-level predictions
- MSA xlsx files (state/metro downloads) would allow per-MSA formula validation
- Kellen offered to get them — defer until we need tighter per-MSA confidence intervals

=== 2026-06-18 Session (continued) — BLS MSA Validation ===

## Full MSA-level formula validation completed

### Data
- Kellen uploaded BLS OEWS MSA files: MSA_M2021 through MSA_M2025 + BOS counterparts
  - MSA files = Metropolitan Statistical Area, cross-industry, cross-ownership estimates
  - BOS files = Balance of State (non-metropolitan areas) — not needed for OFLC
- Mac had all years pre-downloaded at ~/Downloads/oesm{YY}ma/MSA_M20{YY}_dl.xlsx

### Formula validation (19,948 matched BLS MSA × OFLC pairs, 5 years)
- Tested 8 L1 approximation formulas using BLS A_PCT10 and A_PCT25
- Best formula: p10_75_p25_25 (L1 = A_PCT10×0.75 + A_PCT25×0.25)
  - Raw L1 MAE: 7.09%  |  Systematic bias: +2.45% (overestimates)
  - After bias correction (÷1.0245): L1 MAE = 4.2%, bias ≈ 0%
- L4 MAE: ~5% (good for cross-validation but current OFLC data preferred)
- Key finding: publicly published BLS percentiles cannot reconstruct OFLC L1 exactly
  because OFLC uses special BLS tabulations (mean of bottom 1/3), not published percentiles
  The 4-8% residual error is irreducible without access to the special tabulation

### Corrected growth rates (July 2026 projection)
Previous estimates used 5.61%/5.85% — too conservative.
Corrected from full OFLC YoY validation:

Component breakdown:
  ECI Q2 2025 → Q2 2026 (est.): mgmt +3.9%, all +3.6%
  Panel rotation lift (drop Nov2022, add Nov2025): +2.1%
  Historical calibration (3-year OFLC-ECI residual): +0.71%
  Total: mgmt/prof SOCs = +6.71%, all-occ SOCs = +6.41%

OFLC-ECI residuals by year-pair (mean across 10 SOCs, MSA-level):
  2021-22→2022-23: -2.04% (anomalous — ECI spike post-COVID)
  2022-23→2023-24: +0.66%
  2023-24→2024-25: +1.32%
  2024-25→2025-26: +0.14%
  3-year mean (excl. anomalous): +0.71%

### Output
- /tmp/oews_model/oews_july2026_v2.json — 14 SOCs × 18 MSAs
- Widget rebuilt with corrected growth rates and full validation provenance
- Current floors: actual OFLC 2025-26 published wages
- Prediction uncertainty: ±1.5% on growth rate → ±$1,500-4,500 on typical L2 floor

### Remaining gaps
- 2023 national BLS file (national_M2023_dl.xlsx) was never uploaded — minor gap
- Metro MSA data for 2026 itself doesn't exist yet (July 2026 OFLC not yet published)
- NPRM ETA-2026-0001 comment period closed May 26 — if finalized before July 1,
  all four levels would shift to 34th/52nd/70th/88th percentile methodology (much higher)
  Tool notes this but cannot predict finalization timing

=== 2026-06-18 Session (continued) — Upcoming Wage Estimator ===

## Completed this session

### OEWS July 2026 Prevailing Wage Forecaster — full build and Casebase integration

**Data pipeline:**
- Pulled all 619 SOC codes from OFLC 2025-26 ALC_Export.csv with real BLS titles (from MSA_M2025_dl.xlsx)
- Regenerated meta.json with keys sorted by SOC code (previously unsorted — 15-1251 was not adjacent to 15-1252)
- Generated 619 per-SOC wage files: /public/oews/wages/{soc}.json
  Each file: {area_code: [l1, l2, l3, l4, el1, el2, el3, el4]} (cur + est)
- geo.json: 52 states/territories, 1,910 counties, all mapped to CBSA codes via OFLC Geography.csv
- Growth rates: +6.71% (management/professional SOCs), +6.41% (all-occupations SOCs)
- All data files deployed to /Users/Dad/Documents/GitHub/Casebase/frontend/public/oews/

**Standalone test page:**
- /public/wage-forecaster.html — 173 KB, meta.json and geo.json inlined, wage files fetched on demand
- Served via python3 -m http.server 8765 from /public directory
- Fixed SOC sort order bug (meta.json regenerated sorted; JS also sorts defensively)
- Fixed SOC dropdown "stuck open" bug: replaced <select size="8"> (always-visible listbox)
  with a proper React-controlled SocDropdown component (closes on outside click, opens on button click)

**Casebase integration (WageForecaster.jsx + App.jsx):**
- WageForecaster.jsx — 466 lines, full inline style system (var(--text), var(--bg2), var(--amber), etc.)
  matching VisaBulletinView.jsx pattern. No Tailwind.
- SocDropdown component: custom dropdown with search input, grouped by 22 SOC major categories,
  closes on outside click, shows selected label in trigger, clear button when selection active
- App.jsx changes (5 edits):
  1. import { WageForecaster } from "./WageForecaster"
  2. Landing card: "Upcoming Wage Estimator" — green accent (#34d399), bar chart icon
  3. Nav dropdown: added under Tools group
  4. View render: {view === "wage-forecaster" && <WageForecaster />}
  5. Mobile drawer: added under Tools section
- Vite build clean, synced to casebase-app, Docker rebuilt and live

**Key files:**
- /Users/Dad/Documents/GitHub/Casebase/frontend/src/WageForecaster.jsx
- /Users/Dad/Documents/GitHub/Casebase/frontend/src/App.jsx (5 edits)
- /Users/Dad/Documents/GitHub/Casebase/frontend/public/oews/meta.json (619 SOCs, sorted)
- /Users/Dad/Documents/GitHub/Casebase/frontend/public/oews/geo.json (1,910 counties)
- /Users/Dad/Documents/GitHub/Casebase/frontend/public/oews/wages/*.json (619 files)
- /Users/Dad/Documents/GitHub/Casebase/frontend/public/wage-forecaster.html (standalone)

**Pending:**
- Monitor NPRM ETA-2026-0001 — if finalized before July 1, update growth methodology
  and add prominent banner to WageForecaster.jsx
- Consider adding more SOC codes beyond the 619 currently included

=== 2026-06-18 Session (continued) — PWD City/State Parsing Fix ===

## Bug fixed

**Problem**: When a PWD PDF was dropped onto the PERM Comparer, the city field was
receiving the entire "City State Zip" string (e.g., "Richardson TX 75080") instead
of splitting into city and state correctly.

**Root cause**: pdfplumber frequently collapses city/state/zip columns into a
single-space-separated string when extracting text from the ETA-9141 form.
The Layout A parser split on 2+ spaces, so if the PDF had only single spaces
between city/state/zip, the entire string landed in parts[0] (city) with
parts[1] (state) empty.

**Fix**: Added `_US_STATES` set and `_split_city_state_zip(raw_city, raw_state)`
helper to `app/extract_pwd.py`. The helper handles all four failure modes:
  - `"Richardson TX 75080", ""` → strips zip, splits state from city tail
  - `"Richardson TX", ""`       → splits state from city tail
  - `"Richardson", "TX 75080"`  → strips zip from state field
  - `"New York", "NY"`          → already correct, no change

Helper is called at every city/state extraction point in both `_extract_new`
(Layout A + Layout B) and `_extract_old` (Layout A + Layout B).

Unit tests: 10/10 cases passed via Python CLI.

**Files changed**:
- `/Users/Dad/Documents/GitHub/Casebase/app/extract_pwd.py`

**Deploy**: synced via `sync-from-dev.sh` → `docker compose up --build -d` ✓

=== 2026-06-18 Session (continued) — PWD Dual-Wage & Job Title Fixes ===

## Job title fix
The new form (2025+) puts job title on the SAME line as the label with only a
single space separator: "1.Job title * Senior Financial Analyst".
Old regex required \n or 2+ spaces after the asterisk — never matched.
Fix: removed the separator requirement; capture to end of line and trim at "2."
Applied to both _extract_new (F.a.1) and _extract_old (E.a.1).

## Dual-wage fix
ETA-9141 item 4 = wage on minimum requirements; item 5 = wage on alternative
requirements. Both can be present. Item 5 uses DOL fill underscores in the PDF:
"$ 5__9_0_9_3___ ._0_0____" which previous code never extracted.

Changes to app/extract_pwd.py:
- Added pwdWageMin / pwdWageAlt fields to result dict
- Added _parse_wage_amount() helper that strips underscores before parsing
- _extract_new now extracts both item 4 and item 5 separately
- _extract_old same treatment
- pwdWage set to max(wageMin, wageAlt) — the operative floor for PERM
- Frontend banner logic (pwdWageMin != pwdWageAlt) unchanged, still works

Test results across 6 real PWDs:
- P-100-25322: wageMin=$44,387 / wageAlt=$59,093 → displays $59,093 ✓
- P-100-26026: wageMin=$131,082 / wageAlt=$154,606 → displays $154,606 ✓
- All single-wage PWDs unchanged ✓

Files changed: /Users/Dad/Documents/GitHub/Casebase/app/extract_pwd.py
Deploy: synced + docker rebuilt ✓
